# Cinderline legacy Hyprland visual override.
# Intentionally contains NO monitor, workspace, input, animation, bind, or exec rules.
general {
    col.active_border = rgb(e97835) rgb(efb85f) 45deg
    col.inactive_border = rgb(4a3424)
}
